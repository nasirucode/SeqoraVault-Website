import { PreloaderScreen } from '@/components/Preloader'

/** Same preloader UI as the first visit — shown while the next route segment loads. */
export default function Loading() {
  return <PreloaderScreen />
}
